# Build output of `make ui-build` lands here for //go:embed. The
# directory is intentionally non-empty (just this file) so the embed
# directive succeeds on a fresh checkout, before `make ui-build` has
# run. The placeholder in embed.go takes over when no real assets exist.
